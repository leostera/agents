# agents-proc-macros

`agents-proc-macros` contains the proc macros used across the workspace.

It provides:

- `#[derive(Agent)]`
- `#[derive(Tool)]`

These macros expand to the typed runtime in `agents`.
