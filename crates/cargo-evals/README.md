# cargo-evals

`cargo-evals` is the Cargo subcommand for running `agents` eval suites.

It can:

- discover suites generated by `agents::evals::build()`
- list suites and evals
- run filtered evals
- emit inline terminal output or JSON events

## Commands

```bash
cargo evals list
cargo evals models
cargo evals run
cargo evals preserves
```

## Setup

Your crate needs:

- `agents::evals::build()?` in `build.rs`
- `agents::evals::setup!();` in `src/lib.rs`
- suites under `evals/**/*.rs`

With that in place, `cargo evals` will discover and run the generated registry automatically.
