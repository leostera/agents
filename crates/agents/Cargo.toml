[package]
name = "agents"
version = "0.0.1"
edition = "2024"
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Facade crate for building typed Rust agents"
readme = "README.md"

[dependencies]
borg-agent = { path = "../borg-agent", package = "borg-agent", version = "=0.0.1"}
borg-llm = { path = "../borg-llm", package = "borg-llm", version = "=0.0.1"}
agents-macros = { path = "../agents-macros", package = "agents-macros", version = "=0.0.1"}
