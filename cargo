#!/usr/bin/env bash
set -euo pipefail

if command -v sccache >/dev/null 2>&1; then
  export RUSTC_WRAPPER="${RUSTC_WRAPPER:-sccache}"
  # Best-effort: don't fail the build if server management hiccups.
  sccache --start-server >/dev/null 2>&1 || true
fi

exec cargo "$@"
